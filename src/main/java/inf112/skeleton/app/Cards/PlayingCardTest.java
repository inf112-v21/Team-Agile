package inf112.skeleton.app.Cards;

public class PlayingCardTest {

    public static void main(String[] args) {




        //PlayingCard uturn = new PlayingCard(MoveType.UTURN, 20);
        //System.out.println(uturn.getMove());

        /*
        spillerklaseen move(MoveType type) {

        switch(type.getMove()) {



        case(-1)
            switch (spiller.getRotation => 0-360)
            case(spiller.getRotation == 270) {
                setposition(xsamme, y - 1)
            }
            case(90)

            case(180)

            case
        case(1)
        switch (spiller.getRotation => 0-360)
            case(spiller.getRotat) {
                setposition(xsamme, y - 1)
            }
            case(90)

            case(180)
        case(2)
        switch (spiller.getRotation => 0-360)
            case(spiller.getRotat) {
                setposition(xsamme, y - 1)
            }
            case(90)

            case(180)
        case(3)
        switch (spiller.getRotation => 0-360)
            case(spiller.getRotat) {
                setposition(xsamme, y - 1)
            }
            case(90)

            case(180)

        case(180)
        case(90)
        case(-90)







         */





    }
}
